import type { ShowGuesser as RAShowGuesser } from 'react-admin';

type ShowGuesserProps = React.ComponentProps<typeof RAShowGuesser>;

const ShowGuesser = (props: ShowGuesserProps) => {
  console.warn('Not implemented: ShowGuesser');
  return <div>ShowGuesser</div>;
};

export default ShowGuesser;
